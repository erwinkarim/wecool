$('.trackBox').empty();
$('.trackBox').append('<%= escape_javascript( link_to "Follow photos by "+@followed_persona.screen_name, follower_track_path("persona", @followed_persona), :remote=>true, :method=>:post) %>');

<% @mediasets.each do |mediaset| %>
  $('#mediasetlist').append("<%= escape_javascript(link_to mediaset.title, view_sets_path(Persona.find(mediaset.persona_id).screen_name, mediaset)) %>");
<% end %>

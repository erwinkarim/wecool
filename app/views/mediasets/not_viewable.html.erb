
<div class="row-fluid">
  <div class="span12">
    <h2 class="text-center">Opsss..., mediaset not viewable</h2>
  </div>
</div>

<div class="row-fluid">
  <div class="span10 offset1 well" style="height:600px;">
    <!-- handle cases of visible vs. system visible -->
    <p class="text-center">
      <% @mediaset.photos.limit(5).each do |photo| %>
        <%= image_tag photo.avatar.square50, :class=>'img-polaroid' %>
      <% end %>
    </p>
  </div>
</div>

<div class="row-fluid">
  <div class="span12">
    <p class="lead text-center">
      Wish to see a bigger and better version of the mediaset? Kindly 
      <%= link_to 'upgrade', persona_upgrade_acc_path(@persona.screen_name) %>
      <%= @persona == current_persona ? 'Your' : @persona.screen_name+"'s" %> account
    </p>
  </div>
</div>

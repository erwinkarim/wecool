<table class="table">
  <thead>
    <tr>
      <th>Tag</th>
      <th>Count</th> 
    </tr>
  </thead>
  <tbody>
    <% @tags.each do |tag| %>
      <tr>
        <td><%= tag.name %></td>
        <td><%= tag.count %></td>
      </tr>
    <% end %>
  </tbody>
</table>

